# LLM Provider 配置
LLM_PROVIDER=ollama  # 可选: openrouter, ollama

# OpenRouter API 配置（当 LLM_PROVIDER=openrouter 时需要）
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# 默认模型（可选以下任一）
# - anthropic/claude-3.5-sonnet （推荐，质量高）
# - openai/gpt-4o-mini （便宜快速）
# - google/gemini-2.0-flash-exp （免费，限额）
# - meta-llama/llama-3.1-8b-instruct （便宜）
DEFAULT_MODEL=anthropic/claude-3.5-sonnet

# Ollama 配置（当 LLM_PROVIDER=ollama 时使用）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b  # 推荐: qwen2.5:3b, llama3.2:3b, mistral:7b

# ASR 配置
WHISPER_MODEL=tiny  # 可选: tiny(75MB), base(142MB), small(466MB)
WHISPER_LANGUAGE=zh  # 主要语言，auto 为自动检测

# 录音配置
SAMPLE_RATE=16000
SILENCE_THRESHOLD=500  # 静音判断阈值（毫秒）
MAX_RECORDING_TIME=60  # 最大录音时长（秒）
